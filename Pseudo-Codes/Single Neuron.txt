inputs = [3.0, -1.0, 5.0]
weights = [0.1, 0.4, -0.2]
bias = 1.5

output = 0

FOR each (input_value, weight) IN (inputs, weights):
    output = output + (input_value * weight)

output = output + bias

PRINT output
EXPECTED: 0.4